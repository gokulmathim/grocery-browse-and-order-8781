package org.example.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * PUBLIC_INTERFACE
 * Another simple test to verify unit test discovery in CI.
 */
class AnotherSmokeTest {
    @Test
    fun additionWorks() {
        assertEquals(4, 2 + 2)
    }
}

Build instructions

Use the Gradle wrapper in this module's root:
  ./gradlew :app:assembleDebug

If running from the monorepo root, use:
  (cd grocery_ordering_app_frontend && ./gradlew :app:assembleDebug)
